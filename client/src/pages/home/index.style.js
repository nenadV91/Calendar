export default theme => ({
  root: {
    minHeight: theme.sizes.fullHeight,
    position: 'relative'
  },
  loading: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
  }
})